{% doc %}
  @prompt
    Create a responsive product slider section with multiple slides.
    
    Each slide should include:
    
    A large product image taking up most of the width
    
    A small editable description area below or beside the image
    
    Features to include:
    
    A customizable background and image per slide
    
    Optimized for mobile view: content should scale properly and never overflow
    
    Only one slide visible at a time — the first slide should display by default
    
    Add "Next" and "Previous" buttons to navigate through slides
    
    Slides should be stacked, and clicking navigation should smoothly switch to the next slide
    
    No slides should "stick out" or partially show
    
    Allow easy editing of text and images in each slide from the theme editor, dd a smooth fade or slide transition between slides. Use minimal, clean styling with a modern layout. and a top heading for all, the description is not visible in mobile view?, why is it changing so fast, description in mobile view,  ADD SHOP NOW BUTTON THAT LINKS IN EACH SLIDE, THE SLIDER BUTTONS TO BE LITTLE APART FROM EACH OTHER

{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-product-slider-{{ ai_gen_id }} {
    position: relative;
    width: 100%;
    background-color: {{ block.settings.section_background_color }};
    padding: {{ block.settings.section_padding }}px 20px;
    overflow: hidden;
  }

  .ai-product-slider-header-{{ ai_gen_id }} {
    text-align: center;
    margin-bottom: 40px;
  }

  .ai-product-slider-title-{{ ai_gen_id }} {
    color: {{ block.settings.title_color }};
    font-size: {{ block.settings.title_size }}px;
    margin: 0;
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  .ai-product-slider-container-{{ ai_gen_id }} {
    position: relative;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    overflow: hidden;
    border-radius: {{ block.settings.container_border_radius }}px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  }

  .ai-product-slider-track-{{ ai_gen_id }} {
    position: relative;
    width: 100%;
    height: 500px;
  }

  .ai-product-slider-slide-{{ ai_gen_id }} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    visibility: hidden;
    transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);transform: translateX(30px);
    display: flex;
    align-items: center;
    background-color: {{ block.settings.default_slide_background }};
    padding: 60px;
    box-sizing: border-box;
  }

  .ai-product-slider-slide-{{ ai_gen_id }}.active {
    opacity: 1;
    visibility: visible;
    transform: translateX(0);
  }

  .ai-product-slider-content-{{ ai_gen_id }} {
    display: flex;
    align-items: center;
    gap: 60px;
    width: 100%;
    max-width: 100%;
    height: 100%;
  }

  .ai-product-slider-image-wrapper-{{ ai_gen_id }} {
    flex: 1.2;
    max-width: 65%;
    position: relative;
    height: 100%;
    display: flex;
    align-items: center;
  }

  .ai-product-slider-image-{{ ai_gen_id }} {
    width: 100%;
    height: auto;
    max-height: 100%;
    object-fit: cover;
    border-radius: {{ block.settings.image_border_radius }}px;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
  }

  .ai-product-slider-image-placeholder-{{ ai_gen_id }} {
    width: 100%;
    height: 380px;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    border-radius: {{ block.settings.image_border_radius }}px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
  }

  .ai-product-slider-image-placeholder-{{ ai_gen_id }} svg {
    width: 120px;
    height: 120px;
    opacity: 0.3;
    color: #666;
  }

  .ai-product-slider-empty-state-{{ ai_gen_id }} {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    padding: 8px 16px;
    background-color: rgba(255, 255, 255, 0.9);
    color: #666;
    font-size: 12px;
    border-radius: 20px;
    text-align: center;
    pointer-events: none;
    backdrop-filter: blur(10px);
  }

  .ai-product-slider-text-wrapper-{{ ai_gen_id }} {
    flex: 1;
    max-width: 35%;
    padding: 20px 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    height: 100%;
  }

  .ai-product-slider-description-{{ ai_gen_id }} {
    color: {{ block.settings.text_color }};
    font-size: {{ block.settings.text_size }}px;
    line-height: 1.6;
    margin-bottom: 30px;
  }

  .ai-product-slider-description-{{ ai_gen_id }} h1,
  .ai-product-slider-description-{{ ai_gen_id }} h2,
  .ai-product-slider-description-{{ ai_gen_id }} h3 {
    color: {{ block.settings.heading_color }};
    margin-top: 0;
    margin-bottom: 20px;
    font-weight: 600;
    letter-spacing: -0.01em;
  }

  .ai-product-slider-description-{{ ai_gen_id }} h2 {
    font-size:2.2em;
  }

  .ai-product-slider-description-{{ ai_gen_id }} p {
    margin-bottom: 20px;
    opacity: 0.8;
  }

  .ai-product-slider-description-{{ ai_gen_id }} p:last-child {
    margin-bottom: 0;
  }

  .ai-product-slider-shop-button-{{ ai_gen_id }} {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: {{ block.settings.button_padding_y }}px {{ block.settings.button_padding_x }}px;
    background-color: {{ block.settings.shop_button_color }};
    color: {{ block.settings.shop_button_text_color }};
    text-decoration: none;
    border-radius: {{ block.settings.shop_button_border_radius }}px;
    font-size: {{ block.settings.shop_button_text_size }}px;
    font-weight: 600;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    border: {{ block.settings.shop_button_border_width }}px solid {{ block.settings.shop_button_border_color }};
    text-transform: {{ block.settings.shop_button_text_transform }};
    letter-spacing: 0.5px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);min-width: 140px;
  }

  .ai-product-slider-shop-button-{{ ai_gen_id }}:hover {
    background-color: {{ block.settings.shop_button_hover_color }};
    color: {{ block.settings.shop_button_hover_text_color }};
    border-color: {{ block.settings.shop_button_hover_border_color }};
    transform: translateY(-2px);box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
  }

  .ai-product-slider-shop-button-{{ ai_gen_id }}:focus {
    outline: 3px solid {{ block.settings.shop_button_color }};
    outline-offset: 3px;
  }

  .ai-product-slider-navigation-{{ ai_gen_id }} {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background-color: {{ block.settings.nav_button_color }};
    color: {{ block.settings.nav_button_text_color }};
    border: none;
    width: 56px;
    height: 56px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 10;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
    backdrop-filter: blur(10px);
  }

  .ai-product-slider-navigation-{{ ai_gen_id }}:hover {
    background-color: {{ block.settings.nav_button_hover_color }};
    transform: translateY(-50%) scale(1.05);
    box-shadow: 0 6px 25px rgba(0, 0, 0, 0.2);
  }

  .ai-product-slider-navigation-{{ ai_gen_id }}:focus {
    outline: 3px solid {{ block.settings.nav_button_text_color }};
    outline-offset: 3px;
  }

  .ai-product-slider-prev-{{ ai_gen_id }} {
    left: {{ block.settings.nav_button_spacing }}px;
  }

  .ai-product-slider-next-{{ ai_gen_id }} {
    right: {{ block.settings.nav_button_spacing }}px;
  }

  .ai-product-slider-navigation-{{ ai_gen_id }} svg {
    width: 24px;
    height: 24px;
  }

  .ai-product-slider-indicators-{{ ai_gen_id }} {
    display: flex;
    justify-content: center;
    gap: 12px;
    margin-top: 30px;
  }

  .ai-product-slider-indicator-{{ ai_gen_id }} {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: {{ block.settings.indicator_color }};
    opacity: 0.3;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    border: none;
    position: relative;
  }

  .ai-product-slider-indicator-{{ ai_gen_id }}:hover {
    opacity: 0.6;
    transform: scale(1.2);
  }

  .ai-product-slider-indicator-{{ ai_gen_id }}.active {
    opacity: 1;
    background-color: {{ block.settings.indicator_active_color }};transform: scale(1.4);
  }

  .ai-product-slider-indicator-{{ ai_gen_id }}.active::after {
    content: '';
    position: absolute;
    top: -4px;
    left: -4px;
    right: -4px;
    bottom: -4px;
    border: 2px solid {{ block.settings.indicator_active_color }};
    border-radius: 50%;
    opacity: 0.3;
  }

  .ai-product-slider-empty-slider-{{ ai_gen_id }} {
    text-align: center;
    padding: 80px 20px;
    color: #666;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    border-radius: {{ block.settings.container_border_radius }}px;
  }

  .ai-product-slider-empty-slider-{{ ai_gen_id }} h3 {
    margin-bottom: 15px;
    color: #333;
    font-size: 24px;
    font-weight: 600;
  }

  .ai-product-slider-empty-slider-{{ ai_gen_id }} p {
    font-size: 16px;
    opacity: 0.7;
  }

  @media screen and (max-width: 768px) {
    .ai-product-slider-{{ ai_gen_id }} {
      padding: {{ block.settings.section_padding | times: 0.7 }}px 15px;
    }

    .ai-product-slider-title-{{ ai_gen_id }} {
      font-size: {{ block.settings.title_size | times: 0.8 }}px;
    }

    .ai-product-slider-header-{{ ai_gen_id }} {
      margin-bottom: 30px;
    }

    .ai-product-slider-track-{{ ai_gen_id }} {
      height: auto;
      min-height: 500px;
    }

    .ai-product-slider-slide-{{ ai_gen_id }} {
      padding: 20px;
      height: auto;
      min-height: 500px;
      align-items: flex-start;
    }

    .ai-product-slider-content-{{ ai_gen_id }} {
      flex-direction: column;
      gap: 25px;
      text-align: center;
      height: auto;
    }

    .ai-product-slider-image-wrapper-{{ ai_gen_id }} {
      max-width: 100%;
      flex: none;
      height: auto;
      width: 100%;
    }

    .ai-product-slider-text-wrapper-{{ ai_gen_id }} {
      max-width: 100%;
      flex: none;
      padding: 0;height: auto;
      {% if block.settings.show_description_mobile %}
        display: flex;
      {% else %}
        display: none;
      {% endif %}
    }

    .ai-product-slider-image-placeholder-{{ ai_gen_id }} {
      height: 250px;
    }

    .ai-product-slider-image-{{ ai_gen_id }} {
      max-height: 250px;
    }

    .ai-product-slider-description-{{ ai_gen_id }} h2 {
      font-size: 1.6em;
    }

    .ai-product-slider-description-{{ ai_gen_id }} {
      font-size: {{ block.settings.text_size | times: 0.9 }}px;
      margin-bottom: 20px;
    }

    .ai-product-slider-shop-button-{{ ai_gen_id }} {
      padding: {{ block.settings.button_padding_y | times: 0.8 }}px {{ block.settings.button_padding_x | times: 0.8 }}px;
      font-size: {{ block.settings.shop_button_text_size | times: 0.9 }}px;
      min-width: 120px;
    }

    .ai-product-slider-navigation-{{ ai_gen_id }} {
      width: 48px;
      height: 48px;
    }

    .ai-product-slider-navigation-{{ ai_gen_id }} svg {
      width: 20px;
      height: 20px;
    }

    .ai-product-slider-prev-{{ ai_gen_id }} {
      left: {{ block.settings.nav_button_spacing | times: 0.5 }}px;
    }

    .ai-product-slider-next-{{ ai_gen_id }} {
      right: {{ block.settings.nav_button_spacing | times: 0.5 }}px;
    }
  }

  @media screen and (max-width: 480px) {
    .ai-product-slider-slide-{{ ai_gen_id }} {
      padding: 15px;
      min-height: {% if block.settings.show_description_mobile %}500px{% else %}300px{% endif %};
    }

    .ai-product-slider-content-{{ ai_gen_id }} {
      gap: 20px;
    }

    .ai-product-slider-image-placeholder-{{ ai_gen_id }},
    .ai-product-slider-image-{{ ai_gen_id }} {
      max-height: 200px;
    }

    .ai-product-slider-track-{{ ai_gen_id }} {
      min-height: {% if block.settings.show_description_mobile %}500px{% else %}300px{% endif %};
    }

    .ai-product-slider-shop-button-{{ ai_gen_id }} {
      width: 100%;
      max-width: 200px;
    }

    .ai-product-slider-prev-{{ ai_gen_id }} {
      left: {{ block.settings.nav_button_spacing | times: 0.3 }}px;
    }

    .ai-product-slider-next-{{ ai_gen_id }} {
      right: {{ block.settings.nav_button_spacing | times: 0.3 }}px;
    }
  }
{% endstyle %}<product-slider-{{ ai_gen_id }}
  class="ai-product-slider-{{ ai_gen_id }}"
  {{ block.shopify_attributes }}
>
  {% if block.settings.section_title != blank %}
    <div class="ai-product-slider-header-{{ ai_gen_id }}">
      <h2 class="ai-product-slider-title-{{ ai_gen_id }}">{{ block.settings.section_title }}</h2>
    </div>
  {% endif %}

  {% assign slides_count = 0 %}
  {% for i in (1..5) %}
    {% assign slide_enabled_key = 'slide_' | append: i | append: '_enabled' %}
    {% if block.settings[slide_enabled_key] %}
      {% assign slides_count = slides_count | plus: 1 %}
    {% endif %}
  {% endfor %}

  {% if slides_count > 0 %}<div class="ai-product-slider-container-{{ ai_gen_id }}">
      <div class="ai-product-slider-track-{{ ai_gen_id }}" data-track>
        {% assign slide_index = 0 %}
        {% for i in (1..5) %}
          {% liquid
            assign slide_enabled_key = 'slide_' | append: i | append: '_enabled'
            assign slide_image_key = 'slide_' | append: i | append: '_image'
            assign slide_description_key = 'slide_' | append: i | append: '_description'
            assign slide_background_key = 'slide_' | append: i | append: '_background_color'
            assign slide_button_text_key = 'slide_' | append: i | append: '_button_text'
            assign slide_button_link_key = 'slide_' | append: i | append: '_button_link'

            assign slide_enabled = block.settings[slide_enabled_key]
            assign slide_image = block.settings[slide_image_key]
            assign slide_description = block.settings[slide_description_key]
            assign slide_background = block.settings[slide_background_key]
            assign slide_button_text = block.settings[slide_button_text_key]
            assign slide_button_link = block.settings[slide_button_link_key]
          %}

          {% if slide_enabled %}
            <div
              class="ai-product-slider-slide-{{ ai_gen_id }} {% if slide_index == 0 %}active{% endif %}"
              style="background-color: {{ slide_background }};"
              data-slide="{{ slide_index }}"
            >
              <div class="ai-product-slider-content-{{ ai_gen_id }}">
                <div class="ai-product-slider-image-wrapper-{{ ai_gen_id }}">
                  {% if slide_image %}
                    <img
                      src="{{ slide_image | image_url: width: 800 }}"
                      alt="{{ slide_image.alt | escape }}"
                      class="ai-product-slider-image-{{ ai_gen_id }}"loading="lazy"
                      width="{{ slide_image.width }}"
                      height="{{ slide_image.height }}"
                    >
                  {% else %}
                    <div class="ai-product-slider-image-placeholder-{{ ai_gen_id }}">
                      {{ 'product-apparel-1' | placeholder_svg_tag }}<div class="ai-product-slider-empty-state-{{ ai_gen_id }}">
                        Add an image for slide {{ i }}
                      </div>
                    </div>
                  {% endif %}
                </div>

                <div class="ai-product-slider-text-wrapper-{{ ai_gen_id }}">
                  {% if slide_description != blank %}
                    <div class="ai-product-slider-description-{{ ai_gen_id }}">
                      {{ slide_description }}
                    </div>
                  {% else %}
                    <div class="ai-product-slider-description-{{ ai_gen_id }}"><p style="color: #999; font-style: italic;">Add description for slide {{ i }}</p>
                    </div>
                  {% endif %}

                  {% if slide_button_text != blank %}
                    <a
                      href="{{ slide_button_link | default: '#' }}"
                      class="ai-product-slider-shop-button-{{ ai_gen_id }}"
                    >
                      {{ slide_button_text }}
                    </a>
                  {% endif %}
                </div>
              </div>
            </div>
            {% assign slide_index = slide_index | plus: 1 %}
          {% endif %}
        {% endfor %}
      </div>

      {% if slides_count > 1 %}
        <button
          class="ai-product-slider-navigation-{{ ai_gen_id }} ai-product-slider-prev-{{ ai_gen_id }}"
          data-prevaria-label="Previous slide"
        >
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="15,18 9,12 15,6"></polyline>
          </svg>
        </button>

        <button
          class="ai-product-slider-navigation-{{ ai_gen_id }} ai-product-slider-next-{{ ai_gen_id }}"
          data-next
          aria-label="Next slide"
        >
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="9,18 15,12 9,6"></polyline>
          </svg>
        </button>

        <div class="ai-product-slider-indicators-{{ ai_gen_id }}">
          {% for i in (0..slides_count) %}
            {% if i < slides_count %}
              <button
                class="ai-product-slider-indicator-{{ ai_gen_id }} {% if i == 0 %}active{% endif %}"
                data-indicator="{{ i }}"
                aria-label="Go to slide {{ i | plus: 1 }}"></button>
            {% endif %}
          {% endfor %}
        </div>
      {% endif %}
    </div>
  {% else %}
    <div class="ai-product-slider-empty-slider-{{ ai_gen_id }}">
      <h3>Product Slider</h3>
      <p>Enable at least one slide to get started</p>
    </div>
  {% endif %}
</product-slider-{{ ai_gen_id }}><script>
  (function() {
    class ProductSlider{{ ai_gen_id }} extends HTMLElement {
      constructor() {
        super();
        this.currentSlide = 0;
        this.totalSlides = 0;
        this.autoplayInterval = null;
        this.autoplayDuration = {{ block.settings.autoplay_duration | default: 5 }}000;
        this.isAutoplayEnabled = {{ block.settings.enable_autoplay | default: false }};
      }

      connectedCallback() {
        this.track = this.querySelector('[data-track]');
        this.slides = this.querySelectorAll('[data-slide]');
        this.prevButton = this.querySelector('[data-prev]');
        this.nextButton = this.querySelector('[data-next]');
        this.indicators = this.querySelectorAll('[data-indicator]');

        if (!this.track || this.slides.length === 0) return;

        this.totalSlides = this.slides.length;
        this.setupEventListeners();
        
        if (this.isAutoplayEnabled && this.totalSlides > 1) {
          this.startAutoplay();
        }
      }

      disconnectedCallback() {
        this.stopAutoplay();
      }

      setupEventListeners() {
        if (this.prevButton) {
          this.prevButton.addEventListener('click', () => {
            this.stopAutoplay();
            this.prevSlide();
            if (this.isAutoplayEnabled) {
              this.startAutoplay();
            }
          });
        }

        if (this.nextButton) {
          this.nextButton.addEventListener('click', () => {
            this.stopAutoplay();
            this.nextSlide();
            if (this.isAutoplayEnabled) {
              this.startAutoplay();
            }
          });
        }

        this.indicators.forEach((indicator, index) => {
          indicator.addEventListener('click', () => {
            this.stopAutoplay();
            this.goToSlide(index);
            if (this.isAutoplayEnabled) {
              this.startAutoplay();
            }
          });
        });

        let startX = 0;
        let currentX = 0;
        let isDragging = false;

        this.track.addEventListener('touchstart', (e) => {
          startX = e.touches[0].clientX;
          isDragging = true;
          this.stopAutoplay();
        });

        this.track.addEventListener('touchmove', (e) => {
          if (!isDragging) return;
          currentX = e.touches[0].clientX;});

        this.track.addEventListener('touchend', () => {
          if (!isDragging) return;
          isDragging = false;

          const diff = startX - currentX;
          const threshold = 50;

          if (Math.abs(diff) > threshold) {
            if (diff > 0) {
              this.nextSlide();
            } else {
              this.prevSlide();
            }
          }
          
          if (this.isAutoplayEnabled) {
            this.startAutoplay();
          }
        });

        if (this.isAutoplayEnabled) {
          this.addEventListener('mouseenter', () => this.stopAutoplay());
          this.addEventListener('mouseleave', () => this.startAutoplay());
        }
      }

      startAutoplay() {
        if (this.totalSlides <= 1 || !this.isAutoplayEnabled) return;
        this.stopAutoplay();
        this.autoplayInterval = setInterval(() => {
          this.nextSlide();
        }, this.autoplayDuration);
      }

      stopAutoplay() {
        if (this.autoplayInterval) {
          clearInterval(this.autoplayInterval);
          this.autoplayInterval = null;
        }
      }

      prevSlide() {
        this.currentSlide = this.currentSlide === 0 ? this.totalSlides - 1 : this.currentSlide - 1;
        this.updateSlider();
      }

      nextSlide() {
        this.currentSlide = this.currentSlide === this.totalSlides - 1 ? 0 : this.currentSlide + 1;
        this.updateSlider();
      }

      goToSlide(index) {
        this.currentSlide = index;
        this.updateSlider();
      }

      updateSlider() {
        this.slides.forEach((slide, index) => {
          slide.classList.toggle('active', index === this.currentSlide);
        });

        this.indicators.forEach((indicator, index) => {
          indicator.classList.toggle('active', index === this.currentSlide);
        });
      }
    }

    customElements.define('product-slider-{{ ai_gen_id }}', ProductSlider{{ ai_gen_id }});
  })();
</script>

{% schema %}
{
  "name": "Product slider",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Section"
    },
    {
      "type": "text",
      "id": "section_title",
      "label": "Section title",
      "default": "Featured Products"
    },
    {
      "type": "color",
      "id": "section_background_color",
      "label": "Section background",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "section_padding",
      "min": 20,
      "max": 100,
      "step": 10,
      "unit": "px",
      "label": "Section padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "container_border_radius",
      "min": 0,
      "max": 30,
      "step": 2,
      "unit": "px",
      "label": "Container border radius",
      "default": 12
    },
    {
      "type": "color",
      "id": "default_slide_background",
      "label": "Default slide background",
      "default": "#fafafa"
    },
    {
      "type": "header",
      "content": "Mobile settings"
    },
    {
      "type": "checkbox",
      "id": "show_description_mobile",
      "label": "Show description on mobile",
      "default": true
    },
    {
      "type": "header",
      "content": "Autoplay settings"
    },
    {
      "type": "checkbox",
      "id": "enable_autoplay",
      "label": "Enable autoplay",
      "default": false
    },
    {
      "type": "range",
      "id": "autoplay_duration",
      "min": 3,
      "max": 15,
      "step": 1,
      "unit": "s",
      "label": "Autoplay duration",
      "default": 5
    },
    {
      "type": "header",
      "content": "Title style"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "title_size",
      "min": 20,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Title size",
      "default": 36
    },
    {
      "type": "header",
      "content": "Content style"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#333333"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "text_size",
      "min": 14,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Text size",
      "default": 16
    },
    {
      "type": "range",
      "id": "image_border_radius",
      "min": 0,
      "max": 30,
      "step": 2,
      "unit": "px",
      "label": "Image border radius",
      "default": 12
    },
    {
      "type": "header",
      "content": "Shop button style"
    },
    {
      "type": "color",
      "id": "shop_button_color",
      "label": "Button background",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "shop_button_text_color",
      "label": "Button text color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "shop_button_hover_color",
      "label": "Button hover background",
      "default": "#333333"
    },
    {
      "type": "color",
      "id": "shop_button_hover_text_color",
      "label": "Button hover text color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "shop_button_border_color",
      "label": "Button border color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "shop_button_hover_border_color",
      "label": "Button hover border color",
      "default": "#333333"
    },
    {
      "type": "range",
      "id": "shop_button_border_width",
      "min": 0,
      "max": 4,
      "step": 1,
      "unit": "px",
      "label": "Button border width",
      "default": 0
    },
    {
      "type": "range",
      "id": "shop_button_border_radius",
      "min": 0,
      "max": 50,
      "step": 2,
      "unit": "px",
      "label": "Button border radius",
      "default": 6
    },
    {
      "type": "range",
      "id": "shop_button_text_size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Button text size",
      "default": 14
    },
    {
      "type": "range",
      "id": "button_padding_x",
      "min": 15,
      "max": 40,
      "step": 5,
      "unit": "px",
      "label": "Button horizontal padding",
      "default": 25
    },
    {
      "type": "range",
      "id": "button_padding_y",
      "min": 8,
      "max": 20,
      "step": 2,
      "unit": "px",
      "label": "Button vertical padding",
      "default": 12
    },
    {
      "type": "select",
      "id": "shop_button_text_transform",
      "label": "Button text transform",
      "options": [
        {
          "value": "none",
          "label": "Normal"
        },
        {
          "value": "uppercase",
          "label": "Uppercase"
        },
        {
          "value": "lowercase",
          "label": "Lowercase"
        },
        {
          "value": "capitalize",
          "label": "Capitalize"
        }
      ],
      "default": "uppercase"
    },
    {
      "type": "header",
      "content": "Navigation style"
    },
    {
      "type": "range",
      "id": "nav_button_spacing",
      "min": 20,
      "max": 100,
      "step": 10,
      "unit": "px",
      "label": "Navigation button spacing",
      "default": 50
    },
    {
      "type": "color",
      "id": "nav_button_color",
      "label": "Navigation button color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "nav_button_text_color",
      "label": "Navigation button icon color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "nav_button_hover_color",
      "label": "Navigation button hover color",
      "default": "#f5f5f5"
    },
    {
      "type": "color",
      "id": "indicator_color",
      "label": "Indicator color",
      "default": "#cccccc"
    },
    {
      "type": "color",
      "id": "indicator_active_color",
      "label": "Active indicator color",
      "default": "#000000"
    },
    {
      "type": "header",
      "content": "Slide1"
    },
    {
      "type": "checkbox",
      "id": "slide_1_enabled",
      "label": "Enable slide 1",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "slide_1_image",
      "label": "Image"
    },
    {
      "type": "richtext",
      "id": "slide_1_description",
      "label": "Description",
      "default": "<h2>Premium Collection</h2><p>Discover our carefully curated selection of premium products designed for the modern lifestyle.</p>"
    },
    {
      "type": "text",
      "id": "slide_1_button_text",
      "label": "Button text",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "slide_1_button_link",
      "label": "Button link"
    },
    {
      "type": "color",
      "id": "slide_1_background_color",
      "label": "Background color",
      "default": "#fafafa"
    },
    {
      "type": "header",
      "content": "Slide 2"
    },
    {
      "type": "checkbox",
      "id": "slide_2_enabled",
      "label": "Enable slide 2",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "slide_2_image",
      "label": "Image"
    },
    {
      "type": "richtext",
      "id": "slide_2_description",
      "label": "Description",
      "default": "<h2>New Arrivals</h2><p>Explore our latest collection featuring innovative designs and cutting-edge technology.</p>"
    },
    {
      "type": "text",
      "id": "slide_2_button_text",
      "label": "Button text",
      "default": "Explore Collection"
    },
    {
      "type": "url",
      "id": "slide_2_button_link",
      "label": "Button link"
    },
    {
      "type": "color",
      "id": "slide_2_background_color",
      "label": "Background color",
      "default": "#f8f9fa"
    },
    {
      "type": "header",
      "content": "Slide 3"
    },
    {
      "type": "checkbox",
      "id": "slide_3_enabled",
      "label": "Enable slide 3"
    },
    {
      "type": "image_picker",
      "id": "slide_3_image",
      "label": "Image"
    },
    {
      "type": "richtext",
      "id": "slide_3_description",
      "label": "Description",
      "default": "<h2>Limited Edition</h2><p>Exclusive products available for a limited time. Get yours before they're gone.</p>"
    },
    {
      "type": "text",
      "id": "slide_3_button_text",
      "label": "Button text",
      "default": "Shop Limited"
    },
    {
      "type": "url",
      "id": "slide_3_button_link",
      "label": "Button link"
    },
    {
      "type": "color",
      "id": "slide_3_background_color",
      "label": "Background color",
      "default": "#fff8f0"
    },
    {
      "type": "header",
      "content": "Slide 4"
    },
    {
      "type": "checkbox",
      "id": "slide_4_enabled",
      "label": "Enable slide 4"
    },
    {
      "type": "image_picker",
      "id": "slide_4_image",
      "label": "Image"
    },
    {
      "type": "richtext",
      "id": "slide_4_description",
      "label": "Description",
      "default": "<h2>Best Sellers</h2><p>Our most popular products loved by customers worldwide. See what everyone's talking about.</p>"
    },
    {
      "type": "text",
      "id": "slide_4_button_text",
      "label": "Button text",
      "default": "View Best Sellers"
    },
    {
      "type": "url",
      "id": "slide_4_button_link",
      "label": "Button link"
    },
    {
      "type": "color",
      "id": "slide_4_background_color",
      "label": "Background color",
      "default": "#f0f8ff"
    },
    {
      "type": "header",
      "content": "Slide 5"
    },
    {
      "type": "checkbox",
      "id": "slide_5_enabled",
      "label": "Enable slide 5"
    },
    {
      "type": "image_picker",
      "id": "slide_5_image",
      "label": "Image"
    },
    {
      "type": "richtext",
      "id": "slide_5_description",
      "label": "Description",
      "default": "<h2>Sustainable Choice</h2><p>Eco-friendly products that make a positive impact on the environment without compromising quality.</p>"
    },
    {
      "type": "text",
      "id": "slide_5_button_text",
      "label": "Button text",
      "default": "Shop Sustainable"
    },
    {
      "type": "url",
      "id": "slide_5_button_link",
      "label": "Button link"
    },
    {
      "type": "color",
      "id": "slide_5_background_color",
      "label": "Background color",
      "default": "#f0fff4"
    }
  ],
  "presets": [
    {
      "name": "Product slider"
    }
  ]
}
{% endschema %}